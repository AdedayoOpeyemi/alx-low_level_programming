#include <stdio.h>

/**
 * main - Print string using printf
 * Return: return and terminate the code
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
